import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.instalserwis.serwiskotlow',
  appName: 'Serwis Kotłów',
  webDir: 'www'
};

export default config;
